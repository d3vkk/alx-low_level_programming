#include <stdio.h>

/**
 * main - prints all base 10 digits
 *
 * Return: 0 if without error
 */
int main(void)
{
int i = 0;

for (i = 0; i < 10; i++)
{
printf("%d", i);
}
printf("\n");

return (0);
}
